# Send a Line notification on FastAPI 

## Prerequisites

```
pip install --no-cache-dir -r requirements.txt
```

## Usage

```
uvicorn main:app --reload --host 0.0.0.0 --port 8123

# Or 
python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8123
```
